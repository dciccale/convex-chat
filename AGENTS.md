# Agent guidance

Read and follow `CONTRIBUTING.md` before planning or changing this repository.

`convex-chat` is an independent open-source Convex component. Keep application
authentication, relationships, and product policy outside the component.

The workspace uses pnpm and Turborepo. The publishable package lives in
`packages/convex-chat`; runnable examples live in `apps`.
